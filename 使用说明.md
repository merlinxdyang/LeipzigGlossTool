# Merlin's Leipzig Gloss Tool 1.0 使用说明

## 启动

本版本是本地网页，不需要安装 Python 第三方库。

### 方法一：终端启动（推荐）

解压后，在终端进入文件夹：

```bash
cd /你的路径/chinese_gloss_tool_prototype
python3 app.py
```

浏览器会自动打开：

```text
http://127.0.0.1:8765/
```

结束时在终端按 `Control-C`。

### 方法二：双击

如果 macOS 允许执行 `.command` 文件，可双击：

```text
start.command
```

如果系统阻止第一次运行，使用上面的终端方法即可。

### LiteSpeed / PHP 上线（不使用反向代理）

把整个文件夹上传到网站的 `clg` 目录，必须包含 `api` 目录和 `api.php`。线上接口由 PHP 执行，`app.py` 只用于本机运行。服务器需要 PHP 7.4 或更高版本，并启用 `curl`、`json`、`openssl` 扩展。建议同时上传隐藏文件 `.htaccess`，用来阻止源码和测试文件被直接下载；API 本身不依赖 URL 重写。

上传后可用不含真实密钥的请求检查接口：

```bash
curl -i -X POST 'https://你的域名/clg/api/validate/' \
  -H 'Content-Type: application/json' \
  --data '{}'
```

正确结果是 HTTP `400` 和 JSON 内容 `{"ok":false,...}`。如果得到 HTML 网页，请确认 `api/validate/index.php` 和 `api.php` 已完整上传，并确认该目录允许执行 PHP。

## 界面语言

右上角提供三种界面语言：`EN` 表示英文，`简` 表示简体中文，`正` 表示正体中文。简体中文界面默认选择 DeepSeek / `deepseek-v4-flash`；英文和正体中文界面默认选择 OpenAI / `gpt-5.6-luna`。切换界面语言时会应用对应的默认服务商和模型；再次点击当前语言不会覆盖用户手动修改的设置。正体中文界面使用操作系统字体栈；这一设置只影响界面，不会覆盖输出行的字体和字号设置。

## 使用流程

1. 在左侧选择 DeepSeek / OpenAI / Claude / OpenRouter。
2. 输入 API key；点击“验证 API”。
3. 输入或修改模型名。DeepSeek 默认是 `deepseek-v4-flash`；该模型名和 `https://api.deepseek.com` 接口地址与 [DeepSeek 官方快速开始](https://api-docs.deepseek.com/zh-cn/) 一致。
4. 选择普通话、粤语或其他汉语方言，并选择输入是“汉字”还是“拼音 / 罗马字 / IPA”。
5. 每行输入一个已经用空格分好词的例句；空行会被忽略。也可以点击“导入 TXT”，选择一行一个例句的 `.txt` 文件。
6. 点击“生成 Gloss”。
7. 生成结果后，输出预览会直接显示在输入区下方；每个例句都是一个独立表格。
8. 选择需要输出的任意行，并选择“无编号”“连续数字”“字母：(a)”或“字母：a.”。数字起点只能填写正整数；负数、零或无效输入会自动重置为 `1`。两种字母格式都可填写 `a`、`c`、`aa` 等起点，并在 `z` 后继续为 `aa`。
9. 在下方逐例编辑原文、拼音、可选的其他注音、Gloss 和英语自由翻译。每个词项的信息保持成组，并会根据页面宽度自动换行。
10. 在“排版设置”中分别设置每一行的字体、字号、加粗和斜体。
11. 最后可以：
   - 复制无框表格；
   - 复制无框 MD（实际为 Markdown 中可嵌入的无框 HTML table）；
   - 导出透明 SVG；
   - 导出透明 PNG；
   - 保存为 JSON 项目，以后继续编辑。

“载入示例”不需要 API key，可先用它测试编辑和导出功能。

## 拼音与其他注音

- **拼音设置**：普通话可单选“拼音有声调”“拼音数字声调”或“拼音无声调”，默认选择带声调形式。三种模式下轻声都不标 `0`。
- **其他注音设置**：默认留空；可按需选择注音符号、IPA + 数字声调、IPA 固有声调、粤拼 / Jyutping、Yale 或其他体系。

注音符号采用横排 Unicode Bopomofo：一声不标，二、三、四声调号置于音节后，轻声点置于音节前，例如 `ㄨㄛˇ`、`˙ㄉㄜ`。IPA + 数字声调使用 `55/35/214/51` 等调值；IPA 固有声调使用 `˥/˧˥/˨˩˦/˥˩` 等 IPA/赵元任式调符。粤语或自定义方言材料会显示“主要转写体系”。所有生成内容都可人工修改。

## 输出选择与编号

“原文、拼音、其他注音、Gloss、英语自由翻译”可以任意组合。默认选择原文、拼音、Gloss 和英语自由翻译；其他注音默认不选。复选框会同时控制屏幕预览、复制表格、HTML / MD、SVG 和 PNG。英语自由翻译始终与上方第一个词的内容起点对齐。

“无编号”会完全移除编号列。“连续编号”使用整数起始值：起始编号为 `1` 时依次输出 `(1)`、`(2)`、`(3)`；改为 `10` 时依次输出 `(10)`、`(11)`、`(12)`。保存项目时，全部例句、行选择、编号方式和起始编号会一起保存。

## 批量输入与 TXT 导入

输入框中的每一个非空物理行都是一个独立例句。例句内部仍使用空格标记最终词边界。批量生成会按从上到下的顺序逐行请求 API，避免同时发送大量请求；如果中途某一行失败，之前已经完成的例句会保留。

“导入 TXT”只接受扩展名为 `.txt` 的纯文本文件。文件必须一行一个例句，空行会被忽略。导入后文本先进入输入框，用户可以检查或修改，再点击“生成 Gloss”。

多个例句在屏幕预览、富文本复制和 HTML / MD 输出中始终保持为多个独立表格；SVG 和 PNG 使用明显间距分隔各例句。

## 排版设置

五条输出行的默认排版如下：

- 英文和拉丁字符：Times New Roman；
- 中文字符：宋体（macOS 使用 Songti SC / STSong，Windows 回退到 SimSun）；
- 字号：10.5 pt；
- 粗体：关闭；
- 斜体：关闭。

原文、拼音、其他注音、Gloss、英语自由翻译均可独立选择字体、字号、粗体和斜体。预览、复制表格、HTML / MD、SVG 和 PNG 会使用同一套样式。

“保持现有设置”开启时，当前排版会留到下次打开工具继续使用；关闭后不再保存个人排版。“恢复默认”会立即重置全部五行。Gloss 中的全大写语法缩写，如 `SFP`、`PFV`、`3SG`，在富文本预览和导出中自动显示为小型大写字母。“复制表格”专门输出底层小写字母和 Word 可识别的小型大写格式，避免粘贴后只剩首字母为小型大写。

## 当前默认 gloss 原则

- lexical gloss：`book`, `eat`, `already`
- grammatical gloss：`1SG`, `PFV`, `NEG`, `CLF`, `ASP`
- conventional Chinese labels：`BA`, `BEI`, `DE`, `LE`
- proper name：例如 `Zhangsan`
- 不能确定时：保留原形式或只给一个简短候选
- 默认采用实用的 Li & Thompson 式分析
- 当前按要求设置：适用时 `le1 = ASP`, `le2 = PFV`
- 句末助词可统一标为 `SFP`

左侧“Gloss 约定”文本框可以随时修改这些要求；AI 输出后也可以人工任意修改。

## 分词原则

用户输入的空格具有最高优先级。后端会固定输入 token 的数量和顺序。如果 AI 返回的词数与输入不同，系统不会自行重新对齐，而是直接报告错误，要求重新生成或更换模型。

因此，工具不会擅自修改你的分词。

后端会优先使用服务商支持的 JSON / structured output。如果用户自行填写的 OpenAI、Claude 或 OpenRouter 模型明确拒绝该参数，系统会改用普通 JSON 提示重试；认证失败、额度不足和网络错误不会自动重试。

## API key

### 申请入口

- DeepSeek：[API key 申请页面](https://platform.deepseek.com/api_keys)
- OpenAI：[API key 申请页面](https://platform.openai.com/api-keys)
- Claude / Anthropic：[API key 申请页面](https://platform.claude.com/settings/keys)
- OpenRouter：[API key 申请页面](https://openrouter.ai/settings/keys)

申请后充值可用。

**中国境内只能使用 DeepSeek。**

API key 不会写入项目文件，也不会写入浏览器 localStorage。本机模式下，它发送到 `127.0.0.1` 上运行的 Python 后端；线上模式下，它经 HTTPS 发送到同源 `api.php`，再由后端向所选 API 发出请求。程序不记录请求正文。

输入框会请求密码管理器不要自动填充，并会在发送前拦截含空格的明显错误内容。如果验证返回 HTTP 401，请清空输入框，再从服务商的 API-key 管理页面重新复制当前有效的 key；DeepSeek key 可在[官方 API key 页面](https://platform.deepseek.com/api_keys)创建。

线上使用必须保持 HTTPS。本地 8765 端口仍然不应开放到公网。

## 自检

可在项目目录运行无需第三方依赖的回归测试：

```bash
python3 -m unittest -v
node --test test_batch.js test_typography.js test_interface_language.js
php -l api.php
```
