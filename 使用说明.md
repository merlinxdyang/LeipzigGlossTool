# Merlin's Leipzig Gloss Tool 2.0 使用说明

- 工具名：Merlin's Leipzig Gloss Tool
- 版本：2.0
- 开发者：Merlin X. D. Yang
- 意见或建议：可在网页“使用说明”中点击“显示联系邮箱”；文本形式为 `xdyang [at] zjut [dot] edu [dot] cn`，避免公开页面直接暴露可抓取的邮箱地址。
- 打赏：网页“使用说明”内的“支付宝”按钮会打开二维码浮窗；点击浮窗外部或按 `Esc` 关闭。

## 页面

- `https://ailinguistics.cloud/clg/`：普通话、粤语、闽南语及自定义汉语方言。
- `https://ailinguistics.cloud/clg/multilingual.html`：日、德、法、西、荷、梵、维吾尔、蒙古、藏语及自定义语言。

两个工作页面左上端都有完整 AI 服务面板，服务商、模型和本浏览器的加密 API key 在两页之间共用。

## 第一次使用

1. 在任一工作页面左上端找到“AI 服务”。
2. 选择 DeepSeek、OpenAI、Claude 或 OpenRouter，并填写模型名。
3. 输入 API key。
4. 选择仅本次浏览器会话，或主动勾选“在此浏览器记住 90 天”。
5. 点击“验证并保存”。验证成功后输入框会立即清空。
6. 切换到另一个工作页面时，同一配置与凭据会继续使用。

API key 不写入项目文件或 `localStorage`。浏览器保存的是 PHP 使用 AES-256-GCM 生成的认证密文 Cookie，属性为 `Secure`、`HttpOnly`、`SameSite=Strict`。网页 JavaScript 不能读取它。点击“忘记此服务商的 key”可删除对应凭据。

本地 HTTP 预览只有在显式设置 `CLG_ALLOW_INSECURE_LOCAL_COOKIE=1` 且 Host 与客户端地址均为回环地址时，才使用本地测试 Cookie；生产站始终强制 Secure Cookie。两个工作页面调用同一个凭据端点，因此任一页面验证一次后，切换页面不需要再次验证。

## 汉语页面

普通话与粤语保留原有行为。闽南语新增台罗 `Tâi-lô` 和白话字 `Pe̍h-ōe-jī` 入口。

非普通话／国语材料可以在“其他注音设置”选择“汉语释义（逐词对齐）”。该层保持原文语序，每个原文 token 对应一个汉语单元格。

## 多语言页面

1. 选择语言。
2. 若选择“自定义语言”，填写语言名称；默认输出顺序为拉丁转写、IPA、英语 Gloss、汉语 Gloss 和英语自由翻译，并保留可选的汉语自由翻译。
3. 对梵语、维吾尔语和蒙古语等多文字语言选择具体文字变体。
4. 选择主要转写和可选辅助层。
5. 每行输入一个例句，并用空格标出最终词界。
6. 点击“生成 Gloss”。

维吾尔文按逻辑顺序输入，不要插入 RLO、LRO 等 Unicode 方向控制符。页面会负责 RTL 显示。传统蒙古文与西里尔蒙古文是独立文字配置，不能当作可无损互换的字体。

## 输出组合

可独立勾选：

- 原文；
- 主要转写；
- 其他注音；
- 汉语释义；
- Gloss；
- 英语自由翻译；
- 汉语自由翻译。

论文若只需要“原文—原文注音—汉字翻译—汉语自由翻译”，只勾选原文、主要转写、汉语释义和汉语自由翻译即可。

所有行均可逐格编辑，并可独立设置字体、字号、粗体和斜体。输出复选框同时控制屏幕预览、Word 富文本、HTML/MD、SVG 和透明 PNG。

## 分词与分析边界

输入空格具有最高优先级。后端冻结 token 的数量与顺序；AI 返回数量不同会被拒绝。原文 `form` 始终从用户输入恢复，不信任模型改写。

Leipzig 规则中的词缀边界使用 `-`，clitic 使用 `=`。工具不会仅因为一个词表达多个语法特征就虚构形态边界。自动转写、Gloss、汉语释义和翻译仍需研究者复核。

## 保存与导出

- “复制表格”：适合粘贴到 Word／Pages；
- “复制 HTML / MD”：生成无框 HTML table；
- “SVG”：透明、可缩放；
- “透明 PNG”：由 SVG 以 2× 输出；
- “保存”：保存 2.0 JSON 项目，包含语言、文字、输出行、排版、翻译和编辑结果。

1.0 项目仍可打开；缺少的汉语释义和汉语自由翻译字段会初始化为空。

## 部署要求

线上部署需要 PHP 7.4+、`curl`／`json`／`openssl`、HTTPS，以及 Web 根目录之外配置的 `CLG_CREDENTIAL_MASTER_KEY`。生成命令：

```bash
openssl rand -base64 32
```

真实值不得提交到 Git 或写入公开 `.htaccess`。更换主密钥会使旧浏览器 Cookie 全部失效。

## 自检

```bash
python3 -m unittest -v
node --test test_batch.js test_typography.js test_interface_language.js test_language_profiles.js test_ai_service.js
php -l api.php
```
